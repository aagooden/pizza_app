<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

    <h3>Wings</h3>
    <form method="post" action="/on-to-drinks">
			<table>
				<% wings.each do |key, value| %>
					<tr>
						<td>
							<input type="radio" name="selection" value='<%= key %>, <%= value %>' required>
							<%= "$#{value} #{key}" %>
						</td>
						<td>
				      <% if key == "none" %>
								You don't want any
				      <% else %>
				      	<img src="/img/wings.png">
			      	<% end %>
						</td>
					</tr>
				<% end %>
			</table>
Quantity: <input type="number" required min="0" name="quantity">
    <input type="submit" value="Next to Drinks">
	</form>
	<br>
    <a href="/salad"><button>Go back to Salads</button></a>

</body>
</html>
