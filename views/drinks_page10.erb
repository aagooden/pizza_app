<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
    <h3>Drinks</h3>
    <form method="post" action="/on-to-pasta">
			<table>
				<% drinks.each do |key, value| %>
					<tr>
						<td>
							<input type="radio" name="drinks" value='<%= key %>, <%= value %>' required>
							<%= "$#{value} #{key}" %>
						</td>
						<td>
				      <% if key == "none" %>
								You don't want any
				      <% else %>
				      	<img src="/img/<%=key.downcase.gsub(" ", "_")%>.jpg">
			      	<% end %>
						</td>
					</tr>
				<% end %>
			</table>
Quantity: <input type="number" required min="0" name="quantity">
    <input type="submit" value="Next to Pasta">
    </form>

    <br><br>

    <form method="post" action="/back-to-wings">
        <input type="submit" value="Go back to Wings">
    </form>

</body>
</html>
