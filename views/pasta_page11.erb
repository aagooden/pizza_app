<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

    <h3>Pasta</h3>
    <form method="post" action="/on-to-almost-final">
	    <% pasta.each do |key, value| %>
	        <input type="radio" name="pasta" value='<%= key %>, <%= value %>'>
		        <div>
		            <% if key == "Chicken alfredo" %>
                		<%= "#{key} is #{value} dollars" %>
                		<img src="/img/chickenAlfredo.jpg">
                	<% else %>
                		<%= "#{key} is #{value} dollars" %>
                		<img src="/img/lasagna.jpg">
                	<% end %>
		        </div>
		        <br><br>
	    <% end %>
	    <br><br>
    <input type="submit" value="Finalize">
    </form>

    <br><br>

    <form method="post" action="/back-to-drinks">
        <input type="button" value="Go back to Drinks">
    </form>
</body>
</html>