<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

        <h3>Veggies</h3>
        <form method="post" action="/on-to-special-toppings">
    		<% veggies.each do |key, value| %>
	      		<input type="radio" name="veggies" value='<%= key %>, <%= value %>' required>
                <div>
                    <% if key == "Tomato" %>
                        <%= "#{key} is #{value} dollars" %>
                        <img src="/img/tomatoes.jpg">
                    <% elsif key == "Pineapple" %>
                        <%= "#{key} is #{value} dollars" %>
                        <img src="/img/pineapple.jpg">
                    <% else %>
                        <%= "#{key} is #{value} dollars" %>
                        <img src="/img/spinach.jpg">
                    <% end %>
                    </div>

	        	<% end %>

    <input type="submit" value="Next to Special Toppings">
    </form>

    <br><br>
    <form method="post" action="/back-to-meats">
        <input type="button" value="Go back to Meats">
    </form>

</body>
</html>