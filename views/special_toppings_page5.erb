<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

    <h3>Special Toppings</h3>
    <form method="post" action="/on-to-sauces">
    	<% special_toppings.each do |key, value| %>
	   		<input type="radio" name="special_toppings" value='<%= key %>, <%= value %>' required>
                <div>
                    <% if key == "Bbq sauce" %>
                        <%= "the #{key} is #{value} dollars" %>
                        <img src="/img/bbq.jpg">
                    <% elsif key == "none" %>
                        <%= "You don't want any" %> 
                    <% else %>
                        <%= "the #{key} is #{value} dollars" %>
                        <img src="/img/buffaloSauce.jpg">
                    <% end %>
                </div>
        <br>
	    <% end %>
    	<br><br>
    <input type="submit" value="Next to the sauces">
    </form>
    <br><br>

    <form method="post" action="/back-to-veggies">
        <input type="submit" value="Go back to Veggies">
    </form>
</body>
</html>