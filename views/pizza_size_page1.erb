<!DOCTYPE html>
<html>
<head>
	<title>Welcome!</title>
</head>
<body>

	<h1>Scott's Pizza Shack Menu</h1>
	<form method="post" action="/on-to-crust">

		<h3>Pizza Sizes</h3>
	    <% size.each do |key, value| %>
    		<input type="radio" name="size" value='<%= key %>, <%= value %>' required> 
                <% if key == "XL" %>
                    <div>
        		      <%= "an #{key} size pizza starts at #{value} dollars" %>
                      <img src="/img/crust.jpg">
                    </div>
        		<% else %>
                    <div>
        			 <%= "a #{key} size pizza starts at #{value} dollars" %>
                     <img src="/img/crust.jpg">
                    </div>
                    <br>
        		<% end %>
    	<% end %>
        <br><br>
        <div>
    	<input type="submit" value="next to pizza crust">
        </div>
	</form>

    <br><br>
    <input type="button" onclick="location.href='/';" value="Go back to Welcome page">

</body>
</html>