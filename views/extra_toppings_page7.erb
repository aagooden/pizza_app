<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>

    <h3>Extra Toppings</h3>
    <form method="post" action="/on-to-salad">
      <table>
        <% extra_toppings.each do |key, value| %>
          <tr>
            <td>
              <input type="radio" name="selection" value='<%= key %>, <%= value %>' required>
              <%= "$#{value} #{key}" %>
            </td>
            <td>
              <% if key == "none" %>
                You don't want any
              <% elsif key.downcase.include? "double"%>
                <img src="/img/<%=key.downcase.gsub("double ", "")%>.jpg">
              <% else %>
                <img src="/img/<%=key.downcase.gsub(" ", "_")%>.jpg">
              <% end %>
            </td>
          </tr>
        <% end %>
      </table>
		<input type="submit" value="Next to Salads">
    </form>

    <br><br>

    <a href="/sauces"><button>Go back to Sauces</button></a>

</body>
</html>
